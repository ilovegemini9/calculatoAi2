{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev --workspace=@workspace/calculator-platform",
    "build": "npm run build --workspace=@workspace/calculator-platform",
    "start": "npm run start --workspace=@workspace/calculator-platform",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck --workspace=@workspace/calculator-platform",
    "lint": "npm run lint --workspace=@workspace/calculator-platform"
  },
  "workspaces": ["artifacts/*", "lib/*", "scripts"],
  "dependencies": {
    "@google/genai": "^2.12.0",
    "@prisma/adapter-pg": "^7.9.0",
    "@prisma/client": "^7.9.0",
    "prisma": "^7.9.0"
  },
  "devDependencies": {
    "prettier": "^3.9.4",
    "typescript": "~5.9.3"
  },
  "packageManager": "pnpm@10.20.0"
}
