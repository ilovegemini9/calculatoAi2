import assert from 'node:assert/strict';import{circle,gcd,lcm,primeFactors,pythagorean,rightTriangle,surfaceArea}from'../lib/calculators/geometryAdvanced';assert.ok(Math.abs(circle(2).area-12.566370614359172)<1e-12);assert.equal(surfaceArea(1,2,3),22);assert.equal(pythagorean(3,4),5);assert.equal(rightTriangle(5,3).other,4);assert.equal(gcd(18,24),6);assert.equal(lcm(6,8),24);assert.deepEqual(primeFactors(60),[2,2,3,5]);for(const v of[circle(NaN).area,pythagorean(Infinity,4),lcm(0,2)])assert.ok(Number.isFinite(v));console.log('Geometry advanced production tests passed');
