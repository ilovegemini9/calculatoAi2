import assert from 'node:assert/strict';import{convertLength,subnetInfo}from'../lib/calculators/utilityTools';const subnet=subnetInfo('192.168.1.42',24);assert.equal(subnet.valid,true);assert.equal(subnet.network,'192.168.1.0');assert.equal(subnet.broadcast,'192.168.1.255');assert.equal(subnet.firstUsable,'192.168.1.1');assert.equal(subnet.lastUsable,'192.168.1.254');assert.equal(subnet.usableHosts,254);assert.equal(subnetInfo('10.0.0.1',32).totalAddresses,1);assert.equal(subnetInfo('10.0.0.1',32).firstUsable,'10.0.0.1');assert.equal(subnetInfo('bad',24).valid,false);assert.equal(convertLength(1,'meters','feet'),3.280839895013123);assert.equal(convertLength(1,'miles','kilometers'),1.609344);assert.equal(convertLength(NaN,'meters','feet'),0);console.log('Utility tools production tests passed');
