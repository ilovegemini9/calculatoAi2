import assert from 'node:assert/strict';import{convertTimeZone,timeCard}from'../lib/calculators/timeTools';assert.deepEqual(timeCard(5,9,20),{totalHours:45,regularHours:40,overtimeHours:5,grossPay:950});assert.deepEqual(timeCard(0,8,20),{totalHours:0,regularHours:0,overtimeHours:0,grossPay:0});assert.equal(timeCard(5,9,20,8,2).grossPay,1000);assert.equal(convertTimeZone('2026-08-26T12:00','Asia/Tokyo').includes('9:00:00 PM'),true);assert.equal(convertTimeZone('bad','UTC'),'');assert.equal(convertTimeZone('2026-08-26T12:00','Invalid/Zone'),'');console.log('Time tools production tests passed');
