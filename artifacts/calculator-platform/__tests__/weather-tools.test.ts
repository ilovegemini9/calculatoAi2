import assert from 'node:assert/strict';import{dewPoint,heatIndex,windChill}from'../lib/calculators/weatherTools';const wc=windChill(20,15);assert.equal(wc.applicable,true);assert.ok(Math.abs(wc.value-6.2)<0.2);assert.equal(windChill(70,15).applicable,false);const hi=heatIndex(95,60);assert.equal(hi.applicable,true);assert.ok(Math.abs(hi.value-113.09)<0.2);assert.equal(heatIndex(70,60).applicable,false);assert.ok(Math.abs(dewPoint(20,60)-12.0)<0.2);assert.ok(Number.isFinite(dewPoint(-20,0)));console.log('Weather tools production tests passed');
