import assert from 'node:assert/strict';import{gravelVolume,mulchVolume,roofingArea,sleepDuration,tileQuantity}from'../lib/calculators/homeTools';assert.equal(sleepDuration('22:30','06:30'),480);assert.equal(sleepDuration('09:00','17:00'),480);assert.equal(sleepDuration('bad','06:30'),0);const roof=roofingArea(20,10,1.1,10);assert.equal(roof.baseArea,200);assert.ok(Math.abs(roof.pitchedArea-220)<1e-9);assert.ok(Math.abs(roof.orderArea-242)<1e-9);const tile=tileQuantity(10,10,12,12,10);assert.equal(tile.area,100);assert.equal(tile.tileArea,1);assert.equal(tile.tiles,110);assert.equal(mulchVolume(20,10,3),600/324);assert.equal(gravelVolume(0,10,3),0);console.log('Home tools production tests passed');
