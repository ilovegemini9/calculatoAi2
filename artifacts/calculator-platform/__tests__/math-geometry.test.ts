import assert from 'node:assert/strict';import{binary,halfLife,hex,numberSequence,slope,triangle,volume}from'../lib/calculators/mathGeometry';assert.equal(triangle(3,4).area,6);assert.equal(triangle(3,4).hypotenuse,5);assert.equal(volume(2,3,4),24);assert.deepEqual(numberSequence(2,3,4).terms,[2,5,8,11]);assert.equal(binary(10).binary,'1010');assert.equal(hex(255).hex,'FF');assert.equal(halfLife(100,10,10).remaining,50);assert.equal(slope(0,1,2,5).value,2);assert.equal(slope(1,1,1,5).value,0);for(const v of[triangle(NaN,2).area,volume(Infinity,2,3),halfLife(100,0,2).remaining])assert.ok(Number.isFinite(v));console.log('Math geometry production tests passed');
