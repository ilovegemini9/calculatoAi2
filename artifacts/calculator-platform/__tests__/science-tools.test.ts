import assert from 'node:assert/strict';import{density,massConversion,mileage,speed,weightForce}from'../lib/calculators/scienceTools';assert.equal(mileage(12000,12300),300);assert.equal(mileage(12300,12000),0);assert.equal(density(100,20),5);assert.equal(density(100,0),0);const mass=massConversion(70);assert.equal(mass.kilograms,70);assert.ok(Math.abs(mass.pounds-154.3235835295)<1e-9);assert.equal(mass.grams,70000);assert.ok(Math.abs(weightForce(70)-686.4655)<1e-10);assert.equal(weightForce(-2),0);assert.equal(speed(60,2),30);assert.equal(speed(60,0),0);console.log('Science tools production tests passed');
