import assert from 'node:assert/strict';import{area,distance,permutation,probability,sampleSize,summary}from'../lib/calculators/mathStats';assert.ok(Math.abs(area(2).circle-12.566370614359172)<1e-12);assert.equal(distance(0,0,3,4),5);assert.equal(sampleSize(1.96,.5,.05),385);assert.ok(Math.abs(probability(2,4,.5)-.375)<1e-12);const s=summary([1,2,2,4]);assert.equal(s.mean,2.25);assert.equal(s.median,2);assert.equal(s.mode,2);assert.equal(s.range,3);assert.equal(permutation(5,2),20);for(const v of[area(NaN).circle,distance(Infinity,0,1,1),probability(-1,4,.5)])assert.ok(Number.isFinite(v));console.log('Math stats production tests passed');
