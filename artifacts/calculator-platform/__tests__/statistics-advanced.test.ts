import assert from 'node:assert/strict';import{confidenceInterval,meanMedianModeRange,zScore}from'../lib/calculators/statisticsAdvanced';assert.equal(zScore(12,10,2),1);const ci=confidenceInterval(100,10,100);assert.ok(Math.abs(ci.margin-1.96)<1e-12);const s=meanMedianModeRange([1,2,2,5]);assert.equal(s.mean,2.5);assert.equal(s.median,2);assert.equal(s.mode,2);assert.equal(s.range,4);assert.equal(zScore(1,1,0),0);assert.ok(Number.isFinite(confidenceInterval(1,2,0).lower));console.log('Statistics advanced production tests passed');
