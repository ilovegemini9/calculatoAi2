import assert from 'node:assert/strict';import{fuelCost,heatingBtu,squareFootage,voltageDrop}from'../lib/calculators/practicalTools';assert.deepEqual(fuelCost(100,25,3.5),{gallons:4,cost:14});assert.deepEqual(fuelCost(100,0,3.5),{gallons:0,cost:0});const drop=voltageDrop(10,100,1,120);assert.equal(drop.drop,2);assert.equal(drop.remainingVoltage,118);assert.ok(Math.abs(drop.percent-1.6666666667)<1e-9);assert.deepEqual(voltageDrop(10,100,1,0),{drop:2,remainingVoltage:0,percent:0});assert.equal(heatingBtu(1000,30,.5),15000);assert.equal(heatingBtu(-1,30,.5),0);assert.equal(squareFootage(20,10),200);assert.equal(squareFootage(NaN,10),0);console.log('Practical tools production tests passed');
