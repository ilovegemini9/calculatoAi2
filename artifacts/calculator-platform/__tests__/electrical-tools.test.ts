import assert from 'node:assert/strict';import{electricityCost,ohmsLaw,resistorFromBands}from'../lib/calculators/electricalTools';assert.deepEqual(resistorFromBands(2,7,2,5),{ohms:2700,tolerancePercent:5,low:2565,high:2835});assert.equal(resistorFromBands(0,0,0,0).ohms,0);assert.deepEqual(ohmsLaw(120,2),{resistance:60,power:240});assert.deepEqual(ohmsLaw(120,0),{resistance:0,power:0});assert.deepEqual(electricityCost(1000,8,.25),{kilowattHours:8,cost:2});assert.deepEqual(electricityCost(0,8,.25),{kilowattHours:0,cost:0});console.log('Electrical tools production tests passed');
