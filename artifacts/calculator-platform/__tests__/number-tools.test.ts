import assert from 'node:assert/strict';import{average,basic,factors,longDivision,matrixDeterminant,pValue}from'../lib/calculators/numberTools';assert.deepEqual(factors(12),[1,2,3,4,6,12]);assert.equal(basic(6,2,'/'),3);assert.deepEqual(longDivision(17,5),{quotient:3,remainder:2});assert.equal(average([2,4,6]),4);assert.equal(matrixDeterminant([[1,2],[3,4]]),-2);assert.ok(Math.abs(pValue(1.96)-.05)<.002);assert.equal(basic(5,0,'/'),0);assert.equal(average([]),0);for(const v of[factors(Infinity).length,basic(NaN,2,'+'),pValue(NaN)])assert.ok(Number.isFinite(v));console.log('Number tools production tests passed');
