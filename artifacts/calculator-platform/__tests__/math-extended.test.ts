import assert from 'node:assert/strict';import{exponent,logarithm,percentError,root,roundTo}from'../lib/calculators/mathExtended';assert.equal(exponent(2,3),8);assert.equal(root(27,3),3);assert.ok(Math.abs(logarithm(100,10)-2)<1e-12);assert.equal(percentError(105,100),5);assert.equal(roundTo(1.23456,2),1.23);assert.equal(logarithm(-1,10),0);assert.equal(root(9,0),0);const guarded=[exponent(NaN,2),root(9,0),roundTo(Infinity,2)];for(const x of guarded)assert.ok(Number.isFinite(x));console.log('Math extended production tests passed');
