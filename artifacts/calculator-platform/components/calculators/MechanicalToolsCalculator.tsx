/* eslint-disable @typescript-eslint/no-explicit-any */
'use client';import{useMemo,useState}from'react';import{engineHorsepower,horsepowerFromWatts,stairLayout}from'@/lib/calculators/mechanicalTools';import{Field,inputClass,InputsPanel,ResultCard,ResultsPanel}from'./ResultCard';type Mode='horsepower'|'engine-horsepower'|'stair';export function MechanicalToolsCalculator({mode}:{mode:Mode}){const[a,setA]=useState(mode==='stair'?1200:mode==='engine-horsepower'?300:746),[b,setB]=useState(mode==='stair'?7:mode==='engine-horsepower'?5252:0),[c,setC]=useState(mode==='stair'?10:0);const r=useMemo<any>(()=>mode==='horsepower'?horsepowerFromWatts(a):mode==='engine-horsepower'?{hp:engineHorsepower(a,b)}:{...stairLayout(a,b,c)},[mode,a,b,c]);const title=mode==='horsepower'?'Horsepower Calculator':mode==='engine-horsepower'?'Engine Horsepower Calculator':'Stair Calculator';const fields=mode==='horsepower'?[['Power','watts',a,setA,0,1e9]]:mode==='engine-horsepower'?[['Torque','lb-ft',a,setA,0,100000],['Engine speed','RPM',b,setB,0,100000]]:[['Total rise','inches',a,setA,0,100000],['Target riser height','inches',b,setB,0,24],['Tread depth','inches',c,setC,0,48]];return <div className="grid grid-cols-1 lg:grid-cols-2 gap-6"><InputsPanel title={`${title} Inputs`}>{fields.map(([label,unit,value,setter,min,max],i)=><Field key={String(label)} label={`${label} (${unit})`} htmlFor={`${mode}-${i}`}><input id={`${mode}-${i}`} type="number" min={min as number} max={max as number} step={0.01} value={value as number} onChange={e=>(setter as (v:number)=>void)(Number(e.target.value)||0)} className={inputClass}/></Field>)}</InputsPanel><ResultsPanel title={`${title} Results`}>{mode==='horsepower'?<><ResultCard highlight label="Mechanical horsepower" value={`${r.mechanicalHp.toFixed(3)} hp`}/><ResultCard label="Electrical horsepower" value={`${r.electricalHp.toFixed(3)} hp`}/></>:mode==='engine-horsepower'?<ResultCard highlight label="Estimated engine horsepower" value={`${r.hp.toFixed(2)} hp`}/>:<><ResultCard highlight label="Risers / treads" value={`${r.risers} / ${r.treads}`}/><ResultCard label="Actual riser height" value={`${r.actualRiser.toFixed(2)} in`}/><ResultCard label="Total horizontal run" value={`${r.totalRun.toFixed(2)} in`}/><ResultCard label="Stair angle" value={`${r.angle.toFixed(2)}°`}/></>}</ResultsPanel></div>};export const HorsepowerCalculator=()=> <MechanicalToolsCalculator mode="horsepower"/>;export const EngineHorsepowerCalculator=()=> <MechanicalToolsCalculator mode="engine-horsepower"/>;export const StairCalculator=()=> <MechanicalToolsCalculator mode="stair"/>;
