function n(v:number){return Number.isFinite(v)?v:0}export function factors(v:number){const x=Math.abs(Math.trunc(n(v))),out:number[]=[];for(let i=1;i<=x&&i<10000;i++)if(x%i===0)out.push(i);return out}export function basic(a:number,b:number,op:'+'|'-'|'*'|'/'){const x=n(a),y=n(b);return op==='+'?x+y:op==='-'?x-y:op==='*'?x*y:y!==0?x/y:0}export function longDivision(a:number,b:number){const x=n(a),y=n(b);return{quotient:y!==0?Math.trunc(x/y):0,remainder:y!==0?x%y:0}}export function average(values:number[]){const v=values.filter(Number.isFinite);return v.length?v.reduce((s,x)=>s+x,0)/v.length:0}export function pValue(z:number){const x=Math.abs(n(z));const t=1/(1+0.2316419*x),d=0.3989423*Math.exp(-x*x/2);const c=1-d*(0.3193815*t-0.3565638*t*t+1.781478*t**3-1.821256*t**4+1.330274*t**5);return 2*(1-c)}export function matrixDeterminant(a:number[][]){if(a.length!==2||a.some(r=>r.length!==2))return 0;return n(a[0][0])*n(a[1][1])-n(a[0][1])*n(a[1][0])}
