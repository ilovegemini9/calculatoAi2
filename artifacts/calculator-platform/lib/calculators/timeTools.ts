export function timeCard(days:number,hoursPerDay:number,hourlyRate:number,standardHoursPerDay=8,overtimeMultiplier=1.5){const d=Number.isFinite(days)?Math.max(0,days):0,h=Number.isFinite(hoursPerDay)?Math.max(0,hoursPerDay):0,r=Number.isFinite(hourlyRate)?Math.max(0,hourlyRate):0,total=d*h,regular=Math.min(total,d*standardHoursPerDay),overtime=Math.max(0,total-regular);return{totalHours:total,regularHours:regular,overtimeHours:overtime,grossPay:regular*r+overtime*r*overtimeMultiplier};}
export function convertTimeZone(utcInput:string,timeZone:string){const date=new Date(utcInput.endsWith('Z')?utcInput:utcInput+'Z');if(Number.isNaN(date.getTime()))return'';try{return new Intl.DateTimeFormat('en-US',{timeZone, dateStyle:'full',timeStyle:'long'}).format(date);}catch{return'';}}
