const atomicWeights:Record<string,number>={H:1.008,C:12.011,N:14.007,O:15.999,Na:22.99,Cl:35.45,S:32.06,Ca:40.078,Fe:55.845};
export function molarity(moles:number,liters:number){const n=Number.isFinite(moles)?Math.max(0,moles):0,v=Number.isFinite(liters)?Math.max(0,liters):0;return v>0?n/v:0;}
export function molecularWeight(formula:string){let index=0,total=0;while(index<formula.length){const match=/([A-Z][a-z]?)(\d*)/.exec(formula.slice(index));if(!match||match.index!==0)return{valid:false,gramsPerMole:0};const symbol=match[1],count=match[2]?Number(match[2]):1;if(!(symbol in atomicWeights)||!Number.isInteger(count)||count<1)return{valid:false,gramsPerMole:0};total+=atomicWeights[symbol]*count;index+=match[0].length;}return{valid:formula.length>0,gramsPerMole:total};}
export function romanNumeral(value:number){const n=Math.trunc(Number.isFinite(value)?value:0);if(n<1||n>3999)return'';const pairs:[[number,string],[number,string],[number,string],[number,string],[number,string],[number,string],[number,string],[number,string],[number,string]]=[[1000,'M'],[900,'CM'],[500,'D'],[400,'CD'],[100,'C'],[90,'XC'],[50,'L'],[40,'XL'],[10,'X']];let out='',remaining=n;for(const[p,s]of pairs){while(remaining>=p){out+=s;remaining-=p;}}const ones:[[number,string],[number,string],[number,string],[number,string],[number,string],[number,string],[number,string],[number,string],[number,string]]=[[9,'IX'],[5,'V'],[4,'IV'],[1,'I'],[0,''],[0,''],[0,''],[0,''],[0,'']];for(const[p,s]of ones){while(remaining>=p&&p>0){out+=s;remaining-=p;}}return out;}
export function fromRoman(value:string){const map:Record<string,number>={I:1,V:5,X:10,L:50,C:100,D:500,M:1000};let total=0,previous=0;for(const char of value.toUpperCase().split('').reverse()){const current=map[char];if(!current)return 0;total+=current<previous?-current:current;previous=Math.max(previous,current);}return total;}
